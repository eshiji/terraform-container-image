name: Image Tests

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-go@v1
      with:
        go-version: 1.25
    - name: Run Go Tests
      working-directory: tests
      run: go test
